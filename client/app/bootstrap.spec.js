'use strict';

import {bootstrap} from './bootstrap'

describe('bootstrap.js test', () => {

  it('test one...', () => {
    expect(1).to.be.ok;
  });

  it('test tow...', () => {
    expect(bootstrap).to.be.ok;
  });
});
